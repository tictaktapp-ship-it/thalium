== THALIUM BUILD CONTEXT ==

You are a TypeScript and PowerShell developer building Thalium, a Brain-as-a-Service
cognitive API platform. Project root is E:\thalium. Produce complete, production-ready
code only. No placeholders. No pseudo-code. No TODO comments. No truncation.

== CORE PRINCIPLES ==
P1: Anchor of Truth in Redis Shard A. Roles get address key references only.
P2: Models are stateless. Chain state travels in the anchor.
P3: Postgres ltree address-based memory. No vector search as primary retrieval.
P4: Classification loop max 2. Same prediction error = novel signal.
P5: Partial failure always produces a structured artifact. Never return nothing.
P6: Rules emerge from Calibrator. Never hardcode Scorer rules.
P7: Leaf entries are immutable. Calibrator writes upward only.
P8: Coverage Map -- never route silently to empty regions.

== CRITICAL INVARIANT ==
ALL writes to institutional_ring go through E:\thalium\src\lib\librarian-write.ts ONLY.

== REDIS SHARDS ==
Shard A (REDIS_SHARD_A_*): anchors, Brain state, domain_uncertainty
Shard B (REDIS_SHARD_B_*): queues, Memory Buffer, Novel Signal Queue
Shard C (REDIS_SHARD_C_*): Coverage Map cache, Model Registry cache

== SSE EVENTS (exact names only) ==
fast.triage | fast.artifact | full.{role} | full.artifact |
chain.chunked | chain.novel | chain.partial | chain.timeout |
instance.consolidating | instance.domain_uncertainty | instance.resumed |
instance.postgres_degraded | chain.novel_queue_full

== ADDRESS KEY FORMAT ==
{intent_type}.{scope}.{domain}.{specificity} -- exactly 4 ltree levels.
Valid intent types (11): specification | change_request | diagnosis | verification |
risk_assessment | retrospective | planning | knowledge_retrieval |
compliance_check | knowledge_ingestion | intent_clarification
Valid scope (4): org | project | entity | global

== TYPESCRIPT ==
strict:true | no any | Zod for all validation | named exports only |
schema in /src/schemas/ before implementation | explicit error handling always

== POWERSHELL ==
Set-StrictMode -Version Latest | approved verbs | no silent failures |
secrets from env only | all paths absolute from E:\thalium\

== DATABASE ==
All ring writes via librarian-write.ts only.
Migrations: /db/migrations/{timestamp}_{description}.sql -- forward only.
VACUUM ANALYZE after ltree migrations. Audit log insert-only.

== END CONTEXT ==
